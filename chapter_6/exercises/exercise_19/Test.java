/*
Compilar dentro do diretorio exercise_19: javac Test.java
Executar dentro do diretorio exercise_19: java -cp ../ exercise_19.Test
*/

package exercise_19;

import java.time.*;

public class Test {
    public static void main(String[] args) {
       /*
        Given that e refers to an object that implements Predicate, which could be valid code snippets or statements? (Choose all that apply);
       
        A. if(e.test(m))
        B. switch (e.test(m))
        C. while(e.test(m))
        D. e.test(m) ? "yes" : "no";
        E. do {} while(e.test(m));
        F. System.out.print(e.test(m))
        G. boolean b = e.test(m);
       
       */
    }
    // Result: A, C, D, E, F, G
}